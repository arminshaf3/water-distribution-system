{
  "buildCommand": "npm --prefix admin-dashboard install && npm --prefix admin-dashboard run build",
  "outputDirectory": "admin-dashboard/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
